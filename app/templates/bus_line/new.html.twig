{% extends 'base.html.twig' %}

{% block title %}
    {{ 'number.busline_new'|trans }}
{% endblock %}

{% block body %}
    <h1>{{ 'number.busline_new'|trans }}</h1>
    {{ form_start(form, { method: 'POST', action: url('busline_new') }) }}
    {{ form_widget(form) }}
    <div class="form-group row float-sm-right">
        <input type="submit" value="{{ 'action.save'|trans }}" class="btn btn-primary" />
    </div>
    <div class="form-group row float-sm-left">
        <a href="{{ url('busline_index') }}" class="btn btn-link">
            {{ 'action.back_to_list'|trans }}
        </a>
    </div>
    {{ form_end(form) }}
{% endblock %}