{% extends 'base.html.twig' %}

{% block title %}{{ 'title.bus_company_name'|trans }}{% endblock %}

{% block body %}
    <div class="container">
        <div class="searchlink">
            <a href="{{ 'search' }}">{{ 'text.search_for'|trans }}</a>
        </div>
        <div class="viewstops">
            <a href="{{ 'stops' }}">{{ 'text.view_stops'|trans }}</a>
        </div>
        <div class="viewlines">
            <a href="{{ 'lines' }}">{{ 'text.view_lines'|trans }}</a>
        </div>
    </div>
{% endblock %}
