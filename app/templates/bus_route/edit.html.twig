{% extends 'base.html.twig' %}

{% block title %}
    {{ 'title.route_edit'|trans({'%id%': busRoute.id|default('')}) }}
{% endblock %}

{% block body %}
    <h1>{{ 'title.busroute_edit'|trans({'%id%': busRoute.id|default('')}) }}</h1>
    {{ form_start(form, { method: 'PUT', action: url('route_edit', {id: busRoute.id}) }) }}
    {{ form_widget(form) }}
    <div class="form-group row float-sm-right">
        <input type="submit" value="{{ 'action.save'|trans }}" class="btn btn-primary" />
    </div>
    <div class="form-group row float-sm-left">
        <a href="{{ url('route_index') }}" class="btn btn-link">
            {{ 'action.back_to_list'|trans }}
        </a>
    </div>
    {{ form_end(form) }}
{% endblock %}